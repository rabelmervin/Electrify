build:
  command: cargo build --target wasm32-wasip2 --release && mkdir -p ../build && cp ../target/wasm32-wasip2/release/order_processor.wasm ../build/
  component_path: ../build/order_processor.wasm
wit:
  wit_dir: ../wit
